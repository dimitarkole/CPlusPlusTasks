#include<iostream>
using namespace std;
int main()
{
    int n[3];
    cin>>n;
     for(int i;i++;i<=3)

        if(gh==gd||gh==gd||gh==gs||gh==gx||gd==gs||gd==gx||gd==gs||gs==gx)cout<<"Yes";
        else cout<<"No";
}
